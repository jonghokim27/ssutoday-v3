spring:
  config:
    import: classpath:common.yml
  kafka:
    consumer:
      group-id: ${KAFKA_CONSUMER_GROUP:ssutoday}

ssutoday:
  firebase:
    credentials: ${FIREBASE_CREDENTIALS:firebase.json}
